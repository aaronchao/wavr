import { DiscoverPage } from "@/src/features/discover/DiscoverPage";

/** The front door is discovery itself — ranked picks, charts, the works. */
export default function Home() {
  return <DiscoverPage />;
}
